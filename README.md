# AMF Motion Evaluation

AMF-based motion evaluation and cross-model AMF extraction experiments.

## Structure

- `configs/`: experiment configs
- `scripts/`: SLURM scripts
- `src/`: AMF computation, visualization, comparison, and scoring
- `wan_hooks/`: Wan2.2 hook scripts
- `outputs/`: experiment outputs, ignored by git
